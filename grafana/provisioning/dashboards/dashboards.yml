apiVersion: 1

providers:
  - name: DriftScope
    orgId: 1
    folder: DriftScope
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
