[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: exercises FastAPI routes against a real Postgres database
    e2e: full user workflows exercised end-to-end through the HTTP API
